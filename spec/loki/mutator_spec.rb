# frozen_string_literal: true

RSpec.describe Loki::Mutator do
  it "has a version number" do
    expect(Loki::Mutator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
